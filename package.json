{
  "name": "mock-rest-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "copy-files": "copy mockApi/db_back_up.yaml mockApi/db_stage.yaml",
    "tern-on-api": "npx yaml-server --hotReload=off --autoStart=off --port 3000 --database ./mockApi/db_stage.yaml"
  },
  "keywords": ["api","mock","yaml-server","rest"],
  "author": "Andrii Stetsula",
  "license": "ISC",
  "dependencies": {
    "yaml-server": "^1.10.0"
  }
}
